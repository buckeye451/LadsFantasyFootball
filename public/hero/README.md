# Homepage hero assets

Drop your real files here (keep these exact names — they replace the placeholders):

| File | What | Format |
|------|------|--------|
| `player-1.png` | Left-most player cut-out (Cardinals in the mockup) | transparent PNG, portrait ~2:3 |
| `player-2.png` | Player cut-out (Ravens) | transparent PNG |
| `player-3.png` | Player cut-out (Colts) | transparent PNG |
| `player-4.png` | Player cut-out (Seahawks) | transparent PNG |
| `logo.svg` | League badge/logo | SVG preferred (or replace with `logo.png` and tell me) |

Cut-outs should be background-removed, bottom-aligned (feet near the bottom edge),
and at least ~1200px tall. To swap them in from GitHub: open this folder on your
branch → **Add file → Upload files** → drag your files in → commit.
